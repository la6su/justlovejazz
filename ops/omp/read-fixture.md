# OMP read fixture

This immutable fixture validates whether the isolated Worker can read one
explicitly scoped repository file and follow a compact factual contract.

- fixture_id: `omp-read-001`
- engine: `qwen-vllm`
- authority: `Queen`

The expected response contains these three values in the same order and ends
with the Worker invariant. Nothing in this file authorizes implementation,
architecture decisions, external access, shell execution or a broader search.
