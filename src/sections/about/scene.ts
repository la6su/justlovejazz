// Section2 — About: WireframeTypography (3D text). No particles (clean).
import * as THREE from 'three'

export function createSection2(): THREE.Group {
  const g = new THREE.Group()
  g.name = 'about'
  return g
}
