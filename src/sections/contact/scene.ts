// Section4 — Contact: clean, no particles. The glass cube + EnvSphere bg is enough.
import * as THREE from 'three'

export function createSection6(): THREE.Group {
  const g = new THREE.Group()
  g.name = 'contact'
  return g
}
