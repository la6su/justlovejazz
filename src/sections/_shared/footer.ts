// src/sections/_shared/footer.ts — Footer removed (minimalism).
// The joystick is the sole bottom UI element (position:fixed, appends to body
// directly in Experience.ts). No dock bar, no nav, no social — all navigation
// lives in the header dropbar. This file kept as empty export for backward
// compat with page templates that import FOOTER.
export const FOOTER = ''
