// Section0 — Lab (secret left): experiments playground. No particles (clean).
import * as THREE from 'three'

export function createSection0(): THREE.Group {
  const g = new THREE.Group()
  g.name = 'lab'
  return g
}
